%  Latch the random value
ns widmer
1/9/14
%
SUBDESIGN storeit_synch
(	 clock, timesupN, hex_in[3..0]			:INPUT;
	 hex_out[3..0]						:OUTPUT;	)
VARIABLE
	reg[3..0] 	:DFFE;
	
BEGIN	
	reg[].clk = clock;		-- store it on the rising edge of active LOW (end of interval)
	reg[].ena  = timesupN;
	reg[].d = hex_in[];		-- input the random count value
	hex_out[] = reg[].q;	
	
END;
	